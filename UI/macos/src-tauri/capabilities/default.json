{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的最小权限集：核心默认权限 + 自绘标题栏所需的窗口操作。刻意不引入 opener / shell / fs 插件——open_path 与 launch 由 Rust 侧用 std::process::Command 自行实现并做路径白名单。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close"
  ]
}
