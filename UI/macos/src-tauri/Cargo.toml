[package]
name = "claude1-desktop"
version = "0.1.1"
description = "claude1 macOS 桌面端：多渠道 / 多模型状态的只读遥控器"
edition = "2021"
rust-version = "1.85"
publish = false

[lib]
# Tauri v2 惯例：lib 供移动端与集成测试复用，bin 只是薄壳
name = "claude1_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# macos-private-api：窗口透明 + NSVisualEffectView 材质（DESIGN.md 5 节 vibrancy）
tauri = { version = "2", features = ["macos-private-api"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# CC Switch 数据库只读打开；bundled 避免依赖系统 libsqlite3 版本
rusqlite = { version = "0.32", features = ["bundled"] }
dirs = "5"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "s"
strip = "symbols"

# 仓库根另有 Rust workspace（crates/agent-hub），本工程是独立 Tauri 项目，
# 空 [workspace] 表阻止 cargo 向上认亲
[workspace]
